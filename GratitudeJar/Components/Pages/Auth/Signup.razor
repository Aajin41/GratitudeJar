@page "/signup"
@layout AuthLayout
@attribute [Microsoft.AspNetCore.Authorization.AllowAnonymous]

<PageTitle>Citadel — Sign Up</PageTitle>

@if (!string.IsNullOrEmpty(Error))
{
    <div class="alert alert-error">@Error</div>
}

<form method="post" action="/account/signup" class="auth-form">
    <div class="form-group">
        <label class="form-label-plain">Username</label>
        <input type="text" name="username" class="form-input" placeholder="Enter your username" autocomplete="username" required />
    </div>

    <div class="form-group">
        <label class="form-label-plain">Password</label>
        <input type="password" name="password" class="form-input" placeholder="••••••••••••" autocomplete="new-password" required />
    </div>

    <div class="form-group">
        <label class="form-label-plain">Confirm Password</label>
        <input type="password" name="confirm" class="form-input" placeholder="••••••••••••" autocomplete="new-password" required />
    </div>

    <button type="submit" class="btn btn-primary">Sign Up</button>
</form>

<p class="auth-link-text">
    Already have an account? <a href="/login">Log In</a>
</p>

@code {
    [SupplyParameterFromQuery] public string? Error { get; set; }
}
